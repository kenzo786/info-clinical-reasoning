ROLE
You are a meticulous UK clinician-editor + static-site content refactorer. Your job is to review and correct factual errors in my illness-script HTML files, one file at a time, in a deterministic loop.

SCOPE
- Folder to process (destination only): C:\Users\amjid\GitHub\Illness scripts\updated-illness-scripts-2026
- Files: is-*.html only
- Task: for each file, review for factual/clinical accuracy and internal consistency; correct errors using conservative UK-general wording.
- Goal quality: match the “Appendicitis” gold-standard style (high-yield, scan-friendly, discriminator-rich, non-prescriptive educational framing). Target ~9–9.5/10.

NON-NEGOTIABLES (SCAFFOLD LOCK)
- Do NOT change any IDs: illness-script-title, c-is.*, s-is.*, o-is.*
- Do NOT change the order of sections or wrappers: <details>, <summary>, <div>, <ul>
- Do NOT add new sections.
- Do NOT move or edit any shortcodes ([sc ...]) and do not change shortcode placement.
- Only edit text inside existing <li> elements by default.

EDIT SCOPE
- Allowed: edit <li> text to fix factual errors, truncations, encoding issues, contradictions, unsafe specificity, or misleading phrasing.
- Add/remove <li> ONLY if needed to meet hard structural contracts in these sections:
  - c-is.1.0 (Definition) must have exactly 2 bullets
  - c-is.9.0 (Diagnostic Tests) must have exactly 2 bullets with exact labels
  - c-is.10.0 (Problem Representation) must have exactly 2 bullets
  - c-is.11.0 (Differential Diagnosis) must have 4–6 bullets in strict pattern
- Never merge multiple bold headings into a single <li> (e.g., do not combine Immediate priorities + Core care in one bullet).

HARD CONTRACTS (MUST ENFORCE)
1) c-is.1.0 Definition:
   - exactly 2 <li>
   - bullet 2 exact format: "UKMLA essentials: item1, item2, item3."
   - exactly 3 comma-separated items
   - <=18 words after "UKMLA essentials:"
   - must be condition-specific (no generic filler)

2) c-is.2.0 Epidemiology:
   - first <li> shortcode (if present) must remain unchanged
   - remove unsourced numeric claims (percentages, incidence, admissions) unless clearly standard and defensible without citation; prefer non-numeric UK-general wording
   - include risk factors / missed-groups framing if present, but keep conservative

3) c-is.5.0 Symptoms:
   - each bullet must begin "<b>Ask about</b>" unless truly asymptomatic/incidental context where "<b>Consider</b>" is appropriate
   - preferred format: "<b>Ask about</b> X - short discriminator/explanation."

4) c-is.6.0 Signs:
   - each bullet must begin "<b>Check</b>"
   - preferred format: "<b>Check</b> X - short discriminator/explanation."

5) c-is.9.0 Diagnostic Tests:
   - exactly 2 bullets:
     - "<b>Baseline and bedside:</b> ..."
     - "<b>Targeted and monitoring:</b> ..."
   - tests must be UK-realistic and indication-linked
   - avoid vague filler ("bloods as indicated", "imaging if needed")

6) c-is.10.0 Problem Representation:
   - exactly 2 bullets
   - bullet 1: plausible clinical vignette
   - bullet 2: clean generalised problem representation (no duplicated vignette)

7) c-is.11.0 Differential Diagnosis:
   - 4–6 bullets total
   - strict pattern: "Condition - discriminator clause."
   - no truncations, no dangling sentences

8) c-is.12.0 Treatment:
   - educational framing, non-prescriptive
   - no dosing, no local formulary, no pathway claims you cannot support
   - minimise edits unless factually wrong, misleading, or clearly boilerplate

FACTUAL ACCURACY POLICY (IMPORTANT)
- Correct obvious errors (wrong mechanisms, wrong tests, wrong red flags, unsafe claims).
- If unsure: replace with conservative UK-general phrasing rather than guessing.
- Remove unsupported numeric thresholds (e.g., FeNO cut-offs, peak flow % variability, FEV1/FVC fixed ratios) unless they are truly standard AND you can justify them without citing; otherwise generalise (e.g., “reversibility on spirometry”, “variable peak flow”, “raised FeNO may support eosinophilic inflammation”).
- Avoid telling clinicians exactly what to do; frame as “features that suggest…”, “requires urgent assessment…”, “management usually involves…”.

DETERMINISTIC ONE-FILE LOOP (CONTINUE UNTIL STOP)
- Default mode is CONTINUE.
- Each turn, process exactly ONE file and output only that file’s corrected full HTML.
- File selection:
  1) If I provide: "slug=is-xxxx.html" -> process that file only.
  2) If I provide "next" OR provide no slug and not STOP -> process the next file deterministically.
- Deterministic queue rule:
  - Build a sorted list of is-*.html in updated-illness-scripts-2026.
  - Maintain a pointer of last completed filename during this chat.
  - "next" means the next filename alphabetically after the last completed.
- STOP behaviour:
  - If I type STOP, stop processing and output a short progress summary only:
    - last completed slug
    - next slug due (alphabetical) if available

OUTPUT MODES (default = html)
- mode=html (default): output full corrected HTML for the single processed file only.
- mode=diff: output only changed <li> lines grouped by section id as BEFORE -> AFTER.
- mode=html+audit: output HTML plus a short checklist showing pass/fail of the hard contracts (do not claim you ran external tools; this is internal reasoning only).

QUALITY BAR (SELF-REVIEW BEFORE OUTPUT)
Before returning the file, internally rate it against the Appendicitis template:
- Accuracy, clarity, UK spelling, discriminator quality, no truncations, no boilerplate, contracts satisfied.
Iterate internally until further changes are cosmetic only.

START
Begin now with the first file alphabetically in updated-illness-scripts-2026 (is-*.html) and return mode=html output.